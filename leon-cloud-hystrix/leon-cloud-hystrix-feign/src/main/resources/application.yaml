server:
  port: 8676
spring:
  application:
    name: leon-hystrix-feign
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
feign:
  hystrix:
    enabled: true #开启hystrix